import { FormEvent, ReactElement, useState } from "react";
import {
  Activity,
  ArrowDownRight,
  ArrowUpRight,
  Bell,
  CalendarDays,
  Check,
  ChevronDown,
  CircleDollarSign,
  Command,
  Copy,
  FileText,
  Filter,
  Globe2,
  Heart,
  Home,
  LayoutDashboard,
  LogIn,
  MessageCircle,
  MoreHorizontal,
  Paperclip,
  Plus,
  Receipt,
  Search,
  Send,
  Settings2,
  Sparkles,
  Split,
  Sun,
  Target,
  TrendingUp,
  Users,
  WalletCards,
  X,
  Zap,
} from "lucide-react";
import type { ActivityItem, ChatMessage, Expense, Group, Member, SplitMode, View } from "./types";

const groups: Group[] = [
  { id: "copenhagen", name: "Copenhagen trip", emoji: "✦", meta: "Aug 14 – Aug 21, 2025", members: 8, total: 18420, accent: "#b7f36b" },
  { id: "home", name: "Apartment 3B", emoji: "⌂", meta: "Monthly household", members: 4, total: 3260, accent: "#8dd8ff" },
  { id: "studio", name: "Studio crew", emoji: "◌", meta: "Design sprint", members: 6, total: 890, accent: "#ffb1d5" },
];

const members: Member[] = [
  { id: "me", name: "Misbah", initials: "MK", color: "#b7f36b", online: true },
  { id: "sarah", name: "Sarah Chen", initials: "SC", color: "#f7bf6d", online: true },
  { id: "jon", name: "Jon Bell", initials: "JB", color: "#99b8ff" },
  { id: "lina", name: "Lina Park", initials: "LP", color: "#e7a8ff" },
];

const initialExpenses: Expense[] = [
  { id: "hotel", title: "Hotel Nordhavn", category: "Stay", amount: 8400, payer: "Sarah", date: "Today, 10:42", note: "Two nights, waterfront room", receipt: true, status: "Confirmed" },
  { id: "dinner", title: "Dinner at Kødbyen", category: "Food", amount: 2480, payer: "Misbah", date: "Yesterday, 20:18", note: "Shared plates + drinks", receipt: true, status: "Confirmed" },
  { id: "train", title: "Airport train tickets", category: "Transport", amount: 1920, payer: "Jon", date: "Aug 15, 09:05", note: "8 return tickets", status: "Confirmed" },
  { id: "museum", title: "Louisiana Museum", category: "Activities", amount: 1760, payer: "Lina", date: "Aug 15, 14:30", note: "Group admission", receipt: true, status: "Pending" },
];

const initialActivity: ActivityItem[] = [
  { id: "1", member: "Sarah", initials: "SC", action: "added", target: "Hotel Nordhavn", time: "12 min ago", color: "#f7bf6d" },
  { id: "2", member: "Misbah", initials: "MK", action: "confirmed", target: "Dinner at Kødbyen", time: "Yesterday", color: "#b7f36b" },
  { id: "3", member: "Jon", initials: "JB", action: "uploaded a receipt to", target: "Airport train tickets", time: "Yesterday", color: "#99b8ff" },
  { id: "4", member: "Lina", initials: "LP", action: "voted yes on", target: "Book the canal tour", time: "Aug 15", color: "#e7a8ff" },
];

const initialChat: ChatMessage[] = [
  { id: "1", member: "Sarah", initials: "SC", message: "Should we book the canal tour for Friday?", time: "10:31", color: "#f7bf6d" },
  { id: "2", member: "Misbah", initials: "MK", message: "Yes — I found one for kr 3,200 for all of us.", time: "10:33", color: "#b7f36b", mine: true },
  { id: "3", member: "Jon", initials: "JB", message: "I'm in. Add it to the trip budget?", time: "10:35", color: "#99b8ff" },
];

const money = (value: number) => `kr ${value.toLocaleString("en-US")}`;

function Avatar({ member, size = "md" }: { member: Member | { initials: string; color: string }; size?: "sm" | "md" | "lg" }) {
  return <span className={`avatar avatar-${size}`} style={{ background: member.color }}>{member.initials}</span>;
}

function App() {
  const [showLanding, setShowLanding] = useState(true);
  const [activeView, setActiveView] = useState<View>("overview");
  const [activeGroup, setActiveGroup] = useState(groups[0]);
  const [expenses, setExpenses] = useState(initialExpenses);
  const [activity, setActivity] = useState(initialActivity);
  const [chat, setChat] = useState(initialChat);
  const [showExpense, setShowExpense] = useState(false);
  const [showPalette, setShowPalette] = useState(false);
  const [showNotifications, setShowNotifications] = useState(false);
  const [theme, setTheme] = useState<"dark" | "light">("dark");
  const [query, setQuery] = useState("");
  const [toast, setToast] = useState("");

  const notify = (message: string) => {
    setToast(message);
    window.setTimeout(() => setToast(""), 2600);
  };

  const navigate = (view: View) => {
    setActiveView(view);
    setShowLanding(false);
  };

  const addExpense = (expense: Expense) => {
    setExpenses((current) => [expense, ...current]);
    setActivity((current) => [{ id: crypto.randomUUID(), member: "Misbah", initials: "MK", action: "added", target: expense.title, time: "Just now", color: "#b7f36b" }, ...current]);
    setShowExpense(false);
    navigate("expenses");
    notify("Expense saved and balances recalculated");
  };

  const sendMessage = (message: string) => {
    if (!message.trim()) return;
    setChat((current) => [...current, { id: crypto.randomUUID(), member: "Misbah", initials: "MK", message, time: "Now", color: "#b7f36b", mine: true }]);
    notify("Message sent to the group");
  };

  if (showLanding) {
    return <Landing onEnter={() => setShowLanding(false)} onNavigate={navigate} />;
  }

  return (
    <div className={`app-shell ${theme}`}>
      <Sidebar activeView={activeView} onNavigate={navigate} onLanding={() => setShowLanding(true)} activeGroup={activeGroup} onGroupChange={setActiveGroup} onOpenPalette={() => setShowPalette(true)} />
      <main className="main-content">
        <Topbar activeGroup={activeGroup} query={query} setQuery={setQuery} onOpenPalette={() => setShowPalette(true)} onNotifications={() => setShowNotifications((value) => !value)} onTheme={() => setTheme(theme === "dark" ? "light" : "dark")} theme={theme} />
        {showNotifications && <Notifications onClose={() => setShowNotifications(false)} onAction={(message) => { setShowNotifications(false); notify(message); }} />}
        <div className="page-content">
          {activeView === "overview" && <Overview onAddExpense={() => setShowExpense(true)} onNavigate={navigate} expenses={expenses} activity={activity} onToast={notify} />}
          {activeView === "expenses" && <ExpensesPage expenses={expenses} onAddExpense={() => setShowExpense(true)} query={query} onToast={notify} />}
          {activeView === "settle" && <SettlePage onToast={notify} />}
          {activeView === "plan" && <PlanPage onToast={notify} />}
          {activeView === "chat" && <ChatPage chat={chat} onSend={sendMessage} />}
        </div>
      </main>
      <div className="mobile-nav">
        {(["overview", "expenses", "settle", "plan", "chat"] as View[]).map((view) => <button key={view} className={activeView === view ? "active" : ""} onClick={() => navigate(view)}><NavIcon view={view} /><span>{view === "overview" ? "Home" : view === "settle" ? "Settle" : view[0].toUpperCase() + view.slice(1)}</span></button>)}
      </div>
      {showExpense && <ExpenseModal onClose={() => setShowExpense(false)} onSave={addExpense} />}
      {showPalette && <CommandPalette onClose={() => setShowPalette(false)} onNavigate={navigate} onAddExpense={() => { setShowPalette(false); setShowExpense(true); }} />}
      {toast && <div className="toast"><Check size={16} /> {toast}</div>}
    </div>
  );
}

function Landing({ onEnter, onNavigate }: { onEnter: () => void; onNavigate: (view: View) => void }) {
  const [demoTab, setDemoTab] = useState("expenses");
  const [demoSettled, setDemoSettled] = useState(false);
  const demoCards = { expenses: { label: "Expenses", title: "Dinner at Kødbyen", amount: "kr 2,480", detail: "Added by Misbah · 4 participants" }, balance: { label: "Balances", title: "You are owed", amount: "kr 1,060", detail: "Tap to see who owes you" }, chat: { label: "Group chat", title: "The crew is aligned", amount: "3 new", detail: "Create expenses right from chat" } }[demoTab as "expenses" | "balance" | "chat"];
  return <div className="landing">
    <header className="landing-nav"><button className="brand" onClick={onEnter}><span className="brand-mark"><Split size={17} /></span><span>splitwise<span className="brand-plus">+</span></span></button><nav><a href="#product">Product</a><a href="#solutions">Solutions</a><a href="#features">Features</a><a href="#pricing">Pricing</a></nav><div className="nav-actions"><button className="ghost-button" onClick={onEnter}><LogIn size={15} /> Sign in</button><button className="primary-button small" onClick={onEnter}>Get started <ArrowUpRight size={15} /></button></div></header>
    <section className="hero"><div className="hero-copy"><div className="eyebrow"><span className="eyebrow-dot" /> THE SHARED MONEY WORKSPACE</div><h1>Shared money,<br /><em>without</em> the shared headache.</h1><p>Split expenses, plan together, settle up, and keep every shared financial decision in one calm place.</p><div className="hero-actions"><button className="primary-button" onClick={onEnter}>Start for free <ArrowUpRight size={17} /></button><button className="text-button" onClick={() => document.getElementById("product")?.scrollIntoView({ behavior: "smooth" })}>Explore the product <span>↓</span></button></div><div className="hero-proof"><div className="avatar-stack"><Avatar member={members[0]} size="sm" /><Avatar member={members[1]} size="sm" /><Avatar member={members[2]} size="sm" /><Avatar member={members[3]} size="sm" /></div><span>Built for the people<br /><strong>you share life with.</strong></span></div></div><div className="hero-demo" id="product"><div className="demo-glow" /><div className="demo-window"><div className="demo-window-top"><div className="window-dots"><i /><i /><i /></div><span>copenhagen trip</span><span className="demo-lock">⌘ K</span></div><div className="demo-window-body"><div className="demo-mini-sidebar"><span className="active"><LayoutDashboard size={14} /></span><span><Receipt size={14} /></span><span><WalletCards size={14} /></span><span><MessageCircle size={14} /></span></div><div className="demo-panel"><div className="demo-heading"><div><span className="muted-label">AUG 14 – AUG 21, 2025</span><h3>Copenhagen trip <ChevronDown size={14} /></h3></div><span className="demo-members"><Avatar member={members[1]} size="sm" /><Avatar member={members[2]} size="sm" /><Avatar member={members[3]} size="sm" /><b>+5</b></span></div><div className="demo-balance"><span className="muted-label">YOUR BALANCE</span><strong>+ kr 1,060</strong><span>you are owed <ArrowUpRight size={13} /></span></div><div className="demo-tabs">{["expenses", "balance", "chat"].map((tab) => <button key={tab} className={demoTab === tab ? "selected" : ""} onClick={() => setDemoTab(tab)}>{tab === "balance" ? "Balances" : tab[0].toUpperCase() + tab.slice(1)}</button>)}</div><div className="demo-card"><div className="demo-card-icon">{demoTab === "expenses" ? <Receipt size={18} /> : demoTab === "balance" ? <CircleDollarSign size={18} /> : <MessageCircle size={18} />}</div><div><strong>{demoCards.title}</strong><span>{demoCards.detail}</span></div><b>{demoCards.amount}</b></div><div className="demo-card faded"><div className="demo-card-icon soft"><CalendarDays size={18} /></div><div><strong>Canal tour</strong><span>Proposal · 8 people</span></div><button onClick={() => setDemoSettled(!demoSettled)} className="demo-action">{demoSettled ? "Added" : "Vote"}</button></div><div className="demo-chat"><span className="chat-dot" /><span>Sarah is typing…</span><button onClick={() => onNavigate("chat")}><ArrowUpRight size={14} /></button></div></div></div></div></div></section>
    <section className="social-proof"><div><strong>2.4k</strong><span>groups managed</span></div><div><strong>18.6k</strong><span>expenses tracked</span></div><div><strong>৳ 4.2M</strong><span>settled together</span></div><div><strong>24</strong><span>currencies supported</span></div></section>
    <section className="landing-section problem" id="solutions"><div className="section-kicker">THE OLD WAY</div><div className="split-heading"><h2>Money gets messy<br /><em>when it gets shared.</em></h2><p>SplitWise+ turns the awkward questions into clear next steps — without making your group feel like a finance department.</p></div><div className="problem-grid"><div className="problem-card"><span>01</span><h3>“Who paid for the hotel?”</h3><p>See the full story behind every balance. Every expense, participant, receipt, and comment stays connected.</p><button onClick={onEnter}>Explore balances <ArrowUpRight size={15} /></button></div><div className="problem-card active-problem"><span>02</span><h3>“Did everyone pay their share?”</h3><p>Confirm, nudge, and settle from one place. No spreadsheets. No screenshots. No group-chat archaeology.</p><button onClick={onEnter}>See it in action <ArrowUpRight size={15} /></button></div><div className="problem-card"><span>03</span><h3>“Where did the budget go?”</h3><p>Make plans, track spend, and understand what is changing before the trip is over.</p><button onClick={onEnter}>View analytics <ArrowUpRight size={15} /></button></div></div></section>
    <section className="workspace-section" id="features"><div className="workspace-copy"><div className="section-kicker">ONE GROUP. ONE WORKSPACE.</div><h2>Everything shared,<br /><em>finally together.</em></h2><p>Expenses are just the beginning. Bring the conversation, decisions, receipts, plans, and money into the same shared context.</p><div className="feature-list"><button className="feature-item active" onClick={onEnter}><span className="feature-icon lime"><Receipt size={18} /></span><span><strong>Expenses that explain themselves</strong><small>Smart splits, receipts, and confirmations</small></span><ArrowUpRight size={16} /></button><button className="feature-item" onClick={onEnter}><span className="feature-icon blue"><MessageCircle size={18} /></span><span><strong>Conversation with context</strong><small>Turn a message into an action</small></span><ArrowUpRight size={16} /></button><button className="feature-item" onClick={onEnter}><span className="feature-icon pink"><Target size={18} /></span><span><strong>Plans tied to reality</strong><small>Budgets, votes, tasks, and timelines</small></span><ArrowUpRight size={16} /></button></div></div><div className="orbit-visual"><div className="orbit-core"><Split size={27} /><span>copenhagen<br />trip</span></div><div className="orbit-node node-expenses"><Receipt size={17} /><span>Expenses</span></div><div className="orbit-node node-chat"><MessageCircle size={17} /><span>Chat</span></div><div className="orbit-node node-budget"><Target size={17} /><span>Budget</span></div><div className="orbit-node node-plan"><CalendarDays size={17} /><span>Plans</span></div><svg viewBox="0 0 600 500" className="orbit-lines"><ellipse cx="300" cy="250" rx="165" ry="165" /><ellipse cx="300" cy="250" rx="250" ry="130" transform="rotate(-25 300 250)" /></svg></div></section>
    <section className="settlement-section"><div className="settlement-visual"><div className="settlement-before"><span className="muted-label">BEFORE</span><div><b>A</b><span /> <b>B</b><strong>kr 340</strong></div><div><b>B</b><span /> <b>C</b><strong>kr 620</strong></div><div><b>A</b><span /> <b>C</b><strong>kr 210</strong></div><div><b>C</b><span /> <b>D</b><strong>kr 180</strong></div></div><div className="settlement-arrow"><Sparkles size={18} /><ArrowUpRight size={18} /></div><div className="settlement-after"><span className="muted-label">AFTER OPTIMIZATION</span><div><b>A</b><span /> <b>C</b><strong>kr 190</strong></div><div><b>B</b><span /> <b>C</b><strong>kr 430</strong></div><div><b>C</b><span /> <b>D</b><strong>kr 180</strong></div><small>3 payments instead of 5</small></div></div><div className="settlement-copy"><div className="section-kicker">SMART SETTLEMENT</div><h2>Less paying back.<br /><em>More moving on.</em></h2><p>SplitWise+ simplifies the web of shared debts into the fewest, clearest payments — so your group can close the loop together.</p><button className="text-button" onClick={onEnter}>See your balances <ArrowUpRight size={15} /></button></div></section>
    <section className="pricing-section" id="pricing"><div className="section-kicker">SIMPLE BY DESIGN</div><h2>Start together.<br /><em>Grow when you need to.</em></h2><div className="pricing-grid"><div className="price-card"><span className="price-label">FREE</span><h3>For the everyday share</h3><div className="price">$0 <small>/ forever</small></div><p>Everything a small group needs to stay in sync.</p><ul><li><Check size={15} /> Unlimited expenses</li><li><Check size={15} /> Smart settlement</li><li><Check size={15} /> Group chat</li></ul><button className="secondary-button" onClick={onEnter}>Get started</button></div><div className="price-card featured"><span className="price-label">PLUS <i>most loved</i></span><h3>For groups that mean business</h3><div className="price">$4 <small>/ member / month</small></div><p>More space for plans, receipts, budgets, and history.</p><ul><li><Check size={15} /> Everything in Free</li><li><Check size={15} /> Recurring expenses</li><li><Check size={15} /> Advanced analytics</li></ul><button className="primary-button" onClick={onEnter}>Start Plus <ArrowUpRight size={15} /></button></div><div className="price-card"><span className="price-label">TEAMS</span><h3>For communities in motion</h3><div className="price">Let's talk</div><p>Permissions, workspaces, and control for larger groups.</p><ul><li><Check size={15} /> Everything in Plus</li><li><Check size={15} /> Team permissions</li><li><Check size={15} /> Priority support</li></ul><button className="secondary-button" onClick={onEnter}>Contact us</button></div></div></section>
    <footer className="landing-footer"><button className="brand" onClick={onEnter}><span className="brand-mark"><Split size={17} /></span><span>splitwise<span className="brand-plus">+</span></span></button><span>Shared money, without the shared headache.</span><span>© 2025 SplitWise+</span></footer>
  </div>;
}

function Sidebar({ activeView, onNavigate, onLanding, activeGroup, onGroupChange, onOpenPalette }: { activeView: View; onNavigate: (view: View) => void; onLanding: () => void; activeGroup: Group; onGroupChange: (group: Group) => void; onOpenPalette: () => void }) {
  return <aside className="sidebar"><button className="brand app-brand" onClick={onLanding}><span className="brand-mark"><Split size={17} /></span><span>splitwise<span className="brand-plus">+</span></span></button><button className="group-switcher" onClick={() => onGroupChange(groups[(groups.findIndex((group) => group.id === activeGroup.id) + 1) % groups.length])}><span className="group-symbol" style={{ background: activeGroup.accent }}>{activeGroup.emoji}</span><span><small>ACTIVE GROUP</small><strong>{activeGroup.name}</strong></span><ChevronDown size={15} /></button><nav className="side-nav"><span className="nav-section">Workspace</span><NavButton icon={<LayoutDashboard size={17} />} label="Overview" active={activeView === "overview"} onClick={() => onNavigate("overview")} /><NavButton icon={<Receipt size={17} />} label="Expenses" active={activeView === "expenses"} onClick={() => onNavigate("expenses")} badge="4" /><NavButton icon={<WalletCards size={17} />} label="Settle up" active={activeView === "settle"} onClick={() => onNavigate("settle")} /><NavButton icon={<CalendarDays size={17} />} label="Plan" active={activeView === "plan"} onClick={() => onNavigate("plan")} /><NavButton icon={<MessageCircle size={17} />} label="Group chat" active={activeView === "chat"} onClick={() => onNavigate("chat")} badge="3" /><span className="nav-section space-top">Manage</span><NavButton icon={<Target size={17} />} label="Budgets" onClick={() => onNavigate("overview")} /><NavButton icon={<FileText size={17} />} label="Documents" onClick={() => onNavigate("overview")} /><NavButton icon={<Activity size={17} />} label="Activity" onClick={() => onNavigate("overview")} /></nav><div className="sidebar-bottom"><button className="command-hint" onClick={onOpenPalette}><Command size={14} /><span>Quick actions</span><kbd>⌘ K</kbd></button><button className="side-settings"><Settings2 size={16} /><span>Settings</span></button><div className="profile"><Avatar member={members[0]} /><span><strong>Misbah Khan</strong><small>Personal account</small></span><MoreHorizontal size={16} /></div></div></aside>;
}

function NavButton({ icon, label, active, onClick, badge }: { icon: ReactElement; label: string; active?: boolean; onClick: () => void; badge?: string }) { return <button className={`nav-button ${active ? "active" : ""}`} onClick={onClick}>{icon}<span>{label}</span>{badge && <b>{badge}</b>}</button>; }
function NavIcon({ view }: { view: View }) { return view === "overview" ? <Home size={17} /> : view === "expenses" ? <Receipt size={17} /> : view === "settle" ? <WalletCards size={17} /> : view === "plan" ? <CalendarDays size={17} /> : <MessageCircle size={17} />; }

function Topbar({ activeGroup, query, setQuery, onOpenPalette, onNotifications, onTheme, theme }: { activeGroup: Group; query: string; setQuery: (value: string) => void; onOpenPalette: () => void; onNotifications: () => void; onTheme: () => void; theme: string }) {
  return <header className="topbar"><div className="breadcrumbs"><span>Groups</span><b>/</b><strong>{activeGroup.name}</strong></div><div className="topbar-actions"><label className="search-box"><Search size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search anything" /><kbd>⌘ K</kbd></label><button className="icon-button theme-toggle" onClick={onTheme} aria-label="Toggle theme">{theme === "dark" ? <Sun size={17} /> : <Zap size={17} />}</button><button className="icon-button notification-button" onClick={onNotifications} aria-label="Open notifications"><Bell size={17} /><i /></button><button className="invite-button" onClick={onOpenPalette}><Plus size={16} /> Invite</button></div></header>;
}

function Overview({ onAddExpense, onNavigate, expenses, activity, onToast }: { onAddExpense: () => void; onNavigate: (view: View) => void; expenses: Expense[]; activity: ActivityItem[]; onToast: (message: string) => void }) {
  const total = expenses.reduce((sum, expense) => sum + expense.amount, 0);
  return <><div className="page-header"><div><div className="eyebrow muted"><span className="eyebrow-dot" /> AUG 14 – AUG 21, 2025</div><h1>Good morning, Misbah <span>✦</span></h1><p>Here’s what’s happening in your group today.</p></div><button className="primary-button" onClick={onAddExpense}><Plus size={17} /> Add expense</button></div><div className="overview-grid"><div className="balance-card glass-card"><div className="card-topline"><span className="muted-label">YOUR BALANCE</span><button className="more-button" onClick={() => onNavigate("settle")}><MoreHorizontal size={17} /></button></div><div className="balance-number">+ {money(1060)}</div><div className="balance-explainer"><span className="positive-dot" /> You are owed overall <button onClick={() => onNavigate("settle")}>See breakdown <ArrowUpRight size={13} /></button></div><div className="balance-people"><div><Avatar member={members[1]} size="sm" /><span><strong>Sarah owes you</strong><small>{money(840)}</small></span></div><div><Avatar member={members[2]} size="sm" /><span><strong>Jon owes you</strong><small>{money(220)}</small></span></div></div><button className="card-action" onClick={() => onNavigate("settle")}>Review settlements <ArrowUpRight size={15} /></button></div><div className="budget-card glass-card"><div className="card-topline"><span className="muted-label">TRIP SPEND</span><span className="trend positive"><ArrowUpRight size={14} /> 12.4%</span></div><div className="budget-number">{money(total)} <small>of {money(24000)}</small></div><div className="progress-track"><span style={{ width: `${Math.min(total / 24000 * 100, 100)}%` }} /></div><div className="budget-meta"><span>On track for your budget</span><strong>{Math.round(total / 24000 * 100)}%</strong></div><div className="mini-bars"><i style={{ height: "40%" }} /><i style={{ height: "56%" }} /><i style={{ height: "47%" }} /><i style={{ height: "71%" }} /><i style={{ height: "63%" }} /><i style={{ height: "86%" }} /><i className="today" style={{ height: "73%" }} /></div><button className="card-action" onClick={() => onToast("Budget detail view is ready for your group")}>Open budget <ArrowUpRight size={15} /></button></div><div className="members-card glass-card"><div className="card-topline"><span className="muted-label">YOUR GROUP</span><button className="more-button" onClick={() => onToast("Member management opened")}><MoreHorizontal size={17} /></button></div><div className="member-count"><strong>8</strong><span>people<br />in this trip</span></div><div className="member-avatars">{members.map((member) => <Avatar key={member.id} member={member} />)}<span className="avatar-more">+4</span></div><div className="member-online"><span className="positive-dot" /> 4 people are online</div><button className="card-action" onClick={() => onToast("Invite link copied to clipboard")}><Copy size={15} /> Invite someone</button></div></div><div className="section-row"><div><span className="muted-label">RECENT ACTIVITY</span><h2>Keep the group moving.</h2></div><button className="text-button" onClick={() => onToast("Showing all activity")}>View all <ArrowUpRight size={15} /></button></div><div className="lower-grid"><div className="activity-card glass-card">{activity.slice(0, 4).map((item) => <div className="activity-row" key={item.id}><Avatar member={{ initials: item.initials, color: item.color }} size="sm" /><span><strong>{item.member}</strong> {item.action} <b>{item.target}</b><small>{item.time}</small></span><button className="row-arrow" onClick={() => onNavigate("expenses")}><ArrowUpRight size={15} /></button></div>)}</div><div className="insight-card"><div className="insight-icon"><Sparkles size={18} /></div><span className="muted-label">A LITTLE INSIGHT</span><h3>You spend most on<br /><em>shared moments.</em></h3><p>Food makes up 42% of your trip spend so far. Want to see how that compares to your budget?</p><button className="text-button" onClick={() => onNavigate("expenses")}>Explore analytics <ArrowUpRight size={15} /></button></div></div></>;
}

function ExpensesPage({ expenses, onAddExpense, query, onToast }: { expenses: Expense[]; onAddExpense: () => void; query: string; onToast: (message: string) => void }) {
  const [filter, setFilter] = useState("All");
  const filtered = expenses.filter((expense) => `${expense.title} ${expense.category} ${expense.payer}`.toLowerCase().includes(query.toLowerCase()) && (filter === "All" || expense.category === filter));
  return <><div className="page-header"><div><div className="eyebrow muted"><span className="eyebrow-dot" /> GROUP LEDGER</div><h1>Expenses <span className="count-pill">{expenses.length}</span></h1><p>Every shared cost, with the full story attached.</p></div><button className="primary-button" onClick={onAddExpense}><Plus size={17} /> Add expense</button></div><div className="toolbar glass-card"><div className="filter-tabs">{["All", "Food", "Stay", "Transport", "Activities"].map((item) => <button key={item} className={filter === item ? "active" : ""} onClick={() => setFilter(item)}>{item}</button>)}</div><button className="outline-button" onClick={() => onToast("More filters are ready to use")}><Filter size={15} /> Filters</button></div><div className="expense-list glass-card">{filtered.length === 0 ? <EmptyState onAddExpense={onAddExpense} /> : filtered.map((expense) => <div className="expense-row" key={expense.id}><span className={`expense-category ${expense.category.toLowerCase()}`}>{expense.category === "Food" ? "◒" : expense.category === "Stay" ? "⌂" : expense.category === "Transport" ? "↗" : "✦"}</span><span className="expense-main"><strong>{expense.title}</strong><small>{expense.note}</small></span><span className="expense-payer"><small>Paid by</small><strong>{expense.payer}</strong></span><span className="expense-date">{expense.date}</span><span className="expense-status"><i className={expense.status === "Pending" ? "pending" : ""} />{expense.status}</span><strong className="expense-amount">{money(expense.amount)}</strong><button className="row-arrow" onClick={() => onToast(`${expense.title} details opened`)}><ArrowUpRight size={16} /></button></div>)}</div><div className="page-footer-hint"><Sparkles size={15} /> Tip: attach a receipt to make every expense easier to trust.</div></>;
}

function EmptyState({ onAddExpense }: { onAddExpense: () => void }) { return <div className="empty-state"><div className="empty-icon"><Receipt size={22} /></div><h3>No expenses match that filter.</h3><p>Try another category or add the first expense for this group.</p><button className="secondary-button" onClick={onAddExpense}><Plus size={15} /> Add expense</button></div>; }

function SettlePage({ onToast }: { onToast: (message: string) => void }) { return <><div className="page-header"><div><div className="eyebrow muted"><span className="eyebrow-dot" /> CLOSE THE LOOP</div><h1>Settle up <span>↗</span></h1><p>Fewer payments, clearer next steps, no awkward reminders.</p></div><button className="primary-button" onClick={() => onToast("Settlement request sent to Sarah")}>Request settlement <ArrowUpRight size={16} /></button></div><div className="settle-hero glass-card"><div><span className="muted-label">OPTIMIZED GROUP BALANCE</span><strong>3 payments</strong><p>instead of 7 individual IOUs</p></div><div className="settle-spark"><Sparkles size={23} /><span>Smart<br />simplify</span></div><div className="settle-total"><small>GROUP TOTAL TO SETTLE</small><b>{money(1640)}</b></div></div><div className="settle-grid"><div className="glass-card settle-list"><div className="card-heading"><div><span className="muted-label">YOUR NEXT STEPS</span><h2>Recommended transfers</h2></div><button className="more-button" onClick={() => onToast("Settlement options opened")}><MoreHorizontal size={17} /></button></div><TransferRow from="You" to="Sarah" amount={840} color="#f7bf6d" onToast={onToast} /><TransferRow from="You" to="Jon" amount={220} color="#99b8ff" onToast={onToast} /><div className="settled-row"><span className="settled-check"><Check size={15} /></span><span><strong>Lina → You</strong><small>Settled yesterday</small></span><b>{money(580)}</b></div></div><div className="glass-card breakdown-card"><div className="card-heading"><div><span className="muted-label">BALANCE BREAKDOWN</span><h2>Where you stand</h2></div></div><div className="breakdown-visual"><div className="you-node">You<strong>+{money(1060)}</strong></div><div className="connection c1" /><div className="connection c2" /><div className="connection c3" /><span className="node n1">Sarah<small>-{money(840)}</small></span><span className="node n2">Jon<small>-{money(220)}</small></span><span className="node n3">Lina<small>settled</small></span></div><p className="breakdown-note"><Sparkles size={14} /> Your group is 78% settled for this trip.</p></div></div></>;
}
function TransferRow({ from, to, amount, color, onToast }: { from: string; to: string; amount: number; color: string; onToast: (message: string) => void }) { return <div className="transfer-row"><span className="transfer-avatar" style={{ background: color }}>{to.slice(0, 1)}</span><span><strong>{from} <ArrowUpRight size={13} /> {to}</strong><small>Outstanding balance</small></span><b>{money(amount)}</b><button className="settle-button" onClick={() => onToast(`Settlement request sent to ${to}`)}>Request</button></div>; }

function PlanPage({ onToast }: { onToast: (message: string) => void }) { return <><div className="page-header"><div><div className="eyebrow muted"><span className="eyebrow-dot" /> PLAN TOGETHER</div><h1>Trip plan <span>✦</span></h1><p>Keep decisions, deadlines, and spend pointed in the same direction.</p></div><button className="primary-button" onClick={() => onToast("New plan item created")}> <Plus size={17} /> Add to plan</button></div><div className="plan-grid"><div className="glass-card timeline-card"><div className="card-heading"><div><span className="muted-label">UP NEXT</span><h2>Shared itinerary</h2></div><button className="outline-button" onClick={() => onToast("Timeline view opened")}><CalendarDays size={15} /> Timeline</button></div><div className="timeline-item"><div className="timeline-date"><b>16</b><span>AUG</span></div><div className="timeline-copy"><span className="status-chip lime">TODAY</span><h3>Canal tour proposal</h3><p>Vote closes in 4 hours · Budget kr 3,200</p><div className="vote-actions"><button onClick={() => onToast("Vote recorded: yes")}><Check size={14} /> Yes, I’m in</button><button onClick={() => onToast("Maybe vote recorded")}>Maybe</button><span><Avatar member={members[1]} size="sm" /><Avatar member={members[2]} size="sm" /><b>5/8 voted</b></span></div></div></div><div className="timeline-item"><div className="timeline-date"><b>17</b><span>AUG</span></div><div className="timeline-copy"><span className="status-chip blue">BOOKED</span><h3>Louisiana Museum</h3><p>Meet at Central Station · 11:30</p><div className="task-line"><Check size={14} /> Lina added tickets to expenses <ArrowUpRight size={14} /></div></div></div><div className="timeline-item"><div className="timeline-date"><b>19</b><span>AUG</span></div><div className="timeline-copy"><span className="status-chip pink">OPEN</span><h3>Choose dinner spot</h3><p>3 suggestions · Everyone can add a place</p><button className="link-button" onClick={() => onToast("Dinner suggestions opened")}><Plus size={14} /> Add suggestion</button></div></div></div><div className="glass-card task-card"><div className="card-heading"><div><span className="muted-label">GROUP TASKS</span><h2>Who’s on what</h2></div><button className="more-button" onClick={() => onToast("Task actions opened")}><MoreHorizontal size={17} /></button></div><div className="task-person"><Avatar member={members[1]} size="sm" /><span><strong>Sarah</strong><small>Book canal tour</small></span><span className="task-due">Due today</span></div><div className="task-person"><Avatar member={members[2]} size="sm" /><span><strong>Jon</strong><small>Confirm train tickets</small></span><span className="task-due done">Done</span></div><div className="task-person"><Avatar member={members[3]} size="sm" /><span><strong>Lina</strong><small>Upload museum receipt</small></span><span className="task-due">Due Aug 17</span></div><button className="card-action" onClick={() => onToast("New task created")}><Plus size={15} /> Add task</button></div></div></>; }

function ChatPage({ chat, onSend }: { chat: ChatMessage[]; onSend: (message: string) => void }) { return <><div className="page-header"><div><div className="eyebrow muted"><span className="eyebrow-dot" /> LIVE GROUP CHAT</div><h1>Talk it out <span>⌁</span></h1><p>Conversation that knows what your group is doing.</p></div><div className="online-label"><span className="positive-dot" /> 4 online</div></div><div className="chat-layout"><div className="glass-card chat-card"><div className="chat-header"><div><strong>Copenhagen trip</strong><span>8 members · messages become actions</span></div><button className="more-button"><MoreHorizontal size={17} /></button></div><div className="chat-messages">{chat.map((message) => <div className={`chat-message ${message.mine ? "mine" : ""}`} key={message.id}><Avatar member={{ initials: message.initials, color: message.color }} size="sm" /><div><span><strong>{message.member}</strong><small>{message.time}</small></span><p>{message.message}</p>{message.id === "1" && <button className="chat-action" onClick={() => onSend("I voted yes on the canal tour")}>Vote on canal tour <ArrowUpRight size={13} /></button>}</div></div>)}</div><ChatComposer onSend={onSend} /></div><div className="glass-card context-card"><span className="muted-label">SHARED CONTEXT</span><h2>Three things need you.</h2><div className="context-item"><span className="context-icon lime"><Receipt size={15} /></span><span><strong>Hotel Nordhavn</strong><small>Sarah added · confirm expense</small></span><ArrowUpRight size={14} /></div><div className="context-item"><span className="context-icon pink"><Target size={15} /></span><span><strong>Canal tour</strong><small>Vote closes in 4 hours</small></span><ArrowUpRight size={14} /></div><div className="context-item"><span className="context-icon blue"><WalletCards size={15} /></span><span><strong>Your balance</strong><small>You are owed kr 1,060</small></span><ArrowUpRight size={14} /></div><div className="context-tip"><Sparkles size={15} /><span>Try typing <b>“add dinner”</b> to turn a message into an expense.</span></div></div></div></>; }
function ChatComposer({ onSend }: { onSend: (message: string) => void }) { const [value, setValue] = useState(""); const submit = (event: FormEvent) => { event.preventDefault(); onSend(value); setValue(""); }; return <form className="chat-composer" onSubmit={submit}><button type="button"><Paperclip size={16} /></button><input value={value} onChange={(event) => setValue(event.target.value)} placeholder="Message your group…" /><button type="submit" className="send-button"><Send size={16} /></button></form>; }

function ExpenseModal({ onClose, onSave }: { onClose: () => void; onSave: (expense: Expense) => void }) { const [title, setTitle] = useState(""); const [amount, setAmount] = useState(""); const [category, setCategory] = useState("Food"); const [mode, setMode] = useState<SplitMode>("Equal"); const [hasReceipt, setHasReceipt] = useState(false); const save = (event: FormEvent) => { event.preventDefault(); const numericAmount = Number(amount); if (!title.trim() || !numericAmount) return; onSave({ id: crypto.randomUUID(), title, category, amount: numericAmount, payer: "Misbah", date: "Just now", note: mode === "Equal" ? "Split equally across 4 people" : `${mode} split across the group`, receipt: hasReceipt, status: "Pending" }); }; return <div className="modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><form className="modal glass-card" onSubmit={save}><div className="modal-heading"><div><span className="eyebrow muted">NEW TRANSACTION</span><h2>Add an expense</h2><p>Make the split clear before it becomes a question.</p></div><button type="button" className="icon-button" onClick={onClose}><X size={17} /></button></div><label className="field"><span>What was it for?</span><input autoFocus value={title} onChange={(event) => setTitle(event.target.value)} placeholder="e.g. Dinner at Kødbyen" /></label><div className="field-grid"><label className="field"><span>Amount</span><div className="input-prefix"><span>kr</span><input inputMode="decimal" value={amount} onChange={(event) => setAmount(event.target.value.replace(/[^0-9.]/g, ""))} placeholder="0" /></div></label><label className="field"><span>Category</span><select value={category} onChange={(event) => setCategory(event.target.value)}><option>Food</option><option>Stay</option><option>Transport</option><option>Activities</option></select></label></div><div className="field"><span>How should it split?</span><div className="split-options">{(["Equal", "Exact", "Percentage"] as SplitMode[]).map((item) => <button type="button" key={item} className={mode === item ? "active" : ""} onClick={() => setMode(item)}><span>{item === "Equal" ? "÷" : item === "Exact" ? "#" : "%"}</span>{item}</button>)}</div></div><div className="participants"><span className="field-label">Participants <small>4 selected</small></span><div>{members.map((member) => <button type="button" key={member.id} className="participant selected" onClick={() => undefined}><Avatar member={member} size="sm" /><Check size={13} /></button>)}</div><p>Each person pays {amount ? money(Number(amount) / 4) : "kr 0"} with an equal split.</p></div><button type="button" className={`receipt-toggle ${hasReceipt ? "selected" : ""}`} onClick={() => setHasReceipt(!hasReceipt)}><Receipt size={16} /><span><strong>{hasReceipt ? "Receipt attached" : "Attach a receipt"}</strong><small>Make this expense easier to verify</small></span><span className="toggle-dot" /></button><div className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>Cancel</button><button type="submit" className="primary-button"><Check size={16} /> Save expense</button></div></form></div>; }

function Notifications({ onClose, onAction }: { onClose: () => void; onAction: (message: string) => void }) { return <div className="notifications-popover glass-card"><div className="popover-heading"><strong>Notifications</strong><button onClick={onClose}><X size={15} /></button></div><div className="notification-item unread"><span className="notification-icon lime"><Receipt size={15} /></span><span><strong>Sarah added Hotel Nordhavn</strong><small>12 minutes ago · needs confirmation</small></span></div><div className="notification-item"><span className="notification-icon pink"><Target size={15} /></span><span><strong>Canal tour vote closes soon</strong><small>4 hours left · 5 of 8 voted</small></span></div><button className="popover-action" onClick={() => onAction("All notifications marked as read")}>Mark all as read</button></div>; }

function CommandPalette({ onClose, onNavigate, onAddExpense }: { onClose: () => void; onNavigate: (view: View) => void; onAddExpense: () => void }) { const actions = [{ label: "Add an expense", hint: "Create a new shared expense", icon: <Plus size={17} />, action: onAddExpense }, { label: "Open settle up", hint: "Review balances and request transfers", icon: <WalletCards size={17} />, action: () => { onClose(); onNavigate("settle"); } }, { label: "Open group chat", hint: "Talk with your group", icon: <MessageCircle size={17} />, action: () => { onClose(); onNavigate("chat"); } }, { label: "View trip plan", hint: "Votes, tasks, and itinerary", icon: <CalendarDays size={17} />, action: () => { onClose(); onNavigate("plan"); } }]; return <div className="modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><div className="palette glass-card"><div className="palette-input"><Search size={18} /><input autoFocus placeholder="What do you want to do?" /><kbd>ESC</kbd></div><div className="palette-label">QUICK ACTIONS</div>{actions.map((item) => <button className="palette-row" key={item.label} onClick={item.action}><span className="palette-icon">{item.icon}</span><span><strong>{item.label}</strong><small>{item.hint}</small></span><ArrowUpRight size={15} /></button>)}<div className="palette-footer"><span><kbd>↑↓</kbd> Navigate</span><span><kbd>↵</kbd> Select</span><span><kbd>esc</kbd> Close</span></div></div></div>; }

export default App;
